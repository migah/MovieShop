@model IEnumerable<MovieShopDll.Entities.Movie>

@{
    ViewBag.Title = "All movies";
}

<h2>All movies</h2>

<table class="table">
   

@foreach (var item in Model)
{
    <div class="col-md-3 col-sm-6 hero-feature">
        <div class="thumbnail">
          
            <img src="@Html.DisplayFor(modelItem => item.ImageUrl)"/>
            
            <div class="caption">
                <h3>@Html.DisplayFor(modelItem => item.Title)</h3>
                <p>Genre: @Html.DisplayFor(modelItem => item.Genre.Name)</p>
                <p>Pris :@Html.DisplayFor(modelItem => item.Price),- kr</p>
      
                 <p>
                   <a href="/Checkout/" class="btn btn-primary">Buy Now!</a> @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-default" })
                </p>
            </div>
        </div>
    </div>
    
}

</table>
